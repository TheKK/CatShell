language: c
compiler:
        - gcc
        - clang

before_install:
        - sudo apt-get -qq update
install:
        - sudo apt-get -qq install libcmocka-dev

script:
        - make all
        - make dotest
